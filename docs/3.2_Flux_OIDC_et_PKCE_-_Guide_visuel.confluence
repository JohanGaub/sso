h1. 3.2 Comprendre le flux OIDC et PKCE — Guide visuel

*OIDC* (OpenID Connect) est le protocole standard qui permet à une application de déléguer la vérification de l'identité d'un utilisateur à un fournisseur d'identité externe (*IdP*, pour *Identity Provider*) — ici, Microsoft Entra ID.

Ce document vulgarise le fonctionnement de l'authentification OIDC avec Entra ID, pour deux types d'applications : la *web app classique* (avec un serveur backend) et la *Single Page App* (SPA, sans backend).

Il explique aussi *pourquoi* le mécanisme PKCE existe et *comment* il fonctionne (section 3).

{info}
*À qui s'adresse ce document ?*
À tout développeur qui intègre un raccordement SSO.
{info}

----

h2. 1. Avant tout : comprendre les acteurs

Dans un flux OIDC, il y a toujours trois acteurs :

|| Acteur || Rôle || Dans notre contexte ||
|| *L'utilisateur* | Veut accéder à une ressource protégée | Un agent qui se connecte à une application interne |
|| *L'application (client)* | Veut savoir qui est l'utilisateur | Votre app (web app, SPA, etc.) |
|| *Entra ID (IdP)* | Certifie l'identité de l'utilisateur | Microsoft Entra ID — le nouveau SSO cible |

*L'analogie du portier d'hôtel :*
Imaginez que votre application est un hôtel. Elle ne peut pas vérifier elle-même votre identité. Elle fait appel à une agence de sécurité certifiée (Entra ID). L'agence vérifie votre pièce d'identité (le username et le password) et délivre un badge temporaire (l'ID Token). L'hôtel accepte le badge sans avoir à re-vérifier votre identité lui-même.

{tip}Les abréviations techniques des schémas ({{iss}}, {{aud}}, {{exp}}, {{JWKS}}...) sont définies dans *3.1 Correspondance des claims OIDC* ; les définitions générales (IdP, PKCE, ID Token...) dans *0. Migration SSO – Synthèse*, section 4.{tip}

----

h2. 2. Le flux OIDC pour une web app classique (avec backend)

h3. 2.1 Caractéristiques

Une *web app classique* possède un *serveur backend* (ex. Symfony, Spring, Django, Node.js). Ce serveur peut stocker des informations secrètes de manière sécurisée — notamment le {{client_secret}} fourni par l'équipe IAM.

*Analogie du {{client_secret}} :* pensez à un livreur qui doit récupérer un colis dans un immeuble sécurisé. Il ne passe pas par l'accueil visiteurs (le navigateur) — il entre par la porte du personnel (le serveur backend) et présente un badge confidentiel (le {{client_secret}}), connu uniquement de son entreprise et du service de sécurité de l'immeuble. Ce badge ({{client_secret}}) n'est jamais montré aux visiteurs qui passent par le hall (le navigateur) : il circule uniquement entre les deux "back-offices" — le serveur de votre application et Entra ID.

h3. 2.2 Schéma du flux Authorization Code (web app)

!3.2-01-flux-web-app.png|width=900!

{expand:Voir le code source PlantUML (pour modification future)}
{noformat}
@startuml
skinparam sequenceArrowThickness 2
skinparam roundcorner 10
skinparam sequenceParticipantBorderColor #444444
skinparam noteBorderColor #888888
skinparam noteBackgroundColor #FFFDE7

title Flux OIDC – Web App classique (avec backend + client_secret)

actor "Utilisateur" as U #LightBlue
participant "Navigateur" as B #LightGreen
participant "Backend\n(Symfony, Node...)" as App #Orange
participant "Entra ID\n(IdP)" as IdP #LightCoral

autonumber

U -> B : Accède à une URL protégée\n(ex. /tableau-de-bord)
B -> App : GET /tableau-de-bord
App -> B : HTTP 302 → redirect vers Entra ID\n(client_id, redirect_uri, scope, state, nonce)
note right of App
  L'app génère un **state** aléatoire
  (stocké en session pour vérification anti-CSRF)
  et un **nonce** (anti-rejeu dans l'ID Token).
end note

B -> IdP : GET /authorize?client_id=...&state=...&nonce=...
IdP -> B : Affiche la page de connexion Microsoft
U -> IdP : Saisit email + mot de passe (+ MFA si requis)

IdP -> B : HTTP 302 → redirect vers redirect_uri\n?code=CODE_UNIQUE&state=STATE
note right of IdP
  Entra ID génère un **code d'autorisation**
  à usage unique, valable ~60 secondes.
  Ce code seul ne donne accès à rien.
end note

B -> App : GET /callback?code=CODE_UNIQUE&state=STATE
note right of App
  ✅ Vérifie que **state** reçu == state stocké
  (protection contre les attaques CSRF)
end note

App -> IdP : POST /token\n{ code, client_id, **client_secret**, redirect_uri }
note right of App
  Cette requête se fait **serveur → serveur**
  (le navigateur n'y participe pas).
  Le **client_secret** ne transite jamais
  dans le navigateur.
end note

IdP -> App : { id_token, access_token, refresh_token }
note right of App
  L'app valide l'ID Token :
  - Signature (clé publique JWKS)
  - iss, aud, nonce, exp
  Si tout est OK → utilisateur authentifié.
end note

App -> B : Cookie de session (httpOnly, Secure)
B -> U : Affiche /tableau-de-bord ✅

@enduml
{noformat}
{expand}

h3. 2.3 Ce qu'il faut retenir

* *Le navigateur ne voit jamais les tokens* : le {{client_secret}} et les tokens restent côté serveur.
* *Le code d'autorisation* est une chaîne à usage unique, générée par Entra ID juste après la connexion, valable ~60 secondes. Ce n'est ni un mot de passe ni un token : il ne prouve rien à lui seul, il ne sert qu'à être échangé contre les vrais tokens (ID Token, Access Token) auprès d'Entra ID. C'est un concept différent du {{client_secret}} (vu ci-dessus) et du {{code_verifier}}/{{code_challenge}} (vus section 3) — les trois interviennent ensemble mais jouent des rôles différents.
* *La signature de l'ID Token* est vérifiée avant de lui faire confiance (détail des claims contrôlés : *3.1 Correspondance des claims OIDC*).
* *Le {{state}}* protège contre les attaques CSRF (Cross-Site Request Forgery — quelqu'un vous force à lancer un flux d'authentification à votre insu).
* *Le {{nonce}}* protège contre le rejeu de tokens (quelqu'un vous renvoie un ancien ID Token).

----

h2. 3. Le flux OIDC pour une SPA (Single Page App)

h3. 3.1 Le problème fondamental

Une SPA (React, Vue, Angular...) n'a pas de backend propre. *Tout s'exécute dans le navigateur de l'utilisateur.*

*Le problème :* on ne peut pas stocker un {{client_secret}} dans le navigateur. Pourquoi ?

Parce que le code JavaScript d'une SPA est *public* — n'importe qui peut l'inspecter (F12 → Sources). Si vous y mettiez un secret, n'importe qui pourrait le lire et l'utiliser.

{warning}
*Règle absolue :* ne jamais mettre un {{client_secret}} dans le code d'une SPA, d'une app mobile, ou tout ce qui s'exécute côté client (navigateur, application téléchargeable).
{warning}

*Analogie :* imaginez que vous colliez votre code PIN sous votre carte bancaire avec un post-it. Quiconque tient votre carte peut lire le PIN. Une SPA, c'est pareil : le code est "lisible par tous".

h3. 3.2 La solution : PKCE

*PKCE* (Proof Key for Code Exchange, prononcé "pixy") résout ce problème.

*L'idée centrale :* au lieu d'utiliser un secret statique partagé ({{client_secret}}), la SPA génère elle-même un secret temporaire, unique à chaque authentification : le {{code_verifier}}. Elle n'envoie jamais ce secret directement — elle en envoie d'abord une empreinte cryptée (le {{code_challenge}}), et ne révèle le {{code_verifier}} lui-même qu'à la toute fin, au moment d'échanger le code d'autorisation contre les tokens. Entra ID vérifie alors que l'empreinte du {{code_verifier}} reçu correspond bien au {{code_challenge}} initial.

h3. 3.3 Analogie : le vestiaire de musée avec mot de passe

Imaginez que vous allez déposer votre manteau au vestiaire d'un musée. Trois objets interviennent, chacun avec son équivalent technique :

* *Un mot de passe que vous inventez vous-même*, gardé secret — c'est l'équivalent du {{code_verifier}}.
* *Une empreinte de ce mot de passe*, obtenue en le passant dans une moulinette à sens unique (algorithme SHA-256) — c'est l'équivalent du {{code_challenge}}. Comme pour une empreinte digitale, impossible de retrouver le mot de passe en partant de l'empreinte.
* *Un ticket numéroté* que le vestiaire vous remet en échange de votre manteau — c'est l'équivalent du code d'autorisation.

*Déroulé, étape par étape :*

1. *Avant de déposer votre manteau*, vous inventez votre mot de passe ({{code_verifier}}) et calculez son empreinte ({{code_challenge}}). Vous ne donnez au préposé (Entra ID) que *l'empreinte* — jamais le mot de passe. Le préposé la note dans son registre et dépose votre manteau.
2. *Le préposé vous remet un ticket numéroté* (le code d'autorisation). Ce ticket ne suffit pas à lui seul pour récupérer le manteau.
3. *À votre retour*, vous présentez le ticket et récitez votre mot de passe d'origine ({{code_verifier}}). Le préposé recalcule l'empreinte à partir de ce mot de passe et vérifie qu'elle correspond à celle notée dans son registre. Si ça correspond → vous récupérez votre manteau.

*Et si quelqu'un vous vole votre ticket pendant la visite ?*
Un pickpocket se présente au vestiaire avec le ticket volé. Le préposé lui demande de réciter le mot de passe pour confirmer que c'est bien lui qui a déposé le manteau. Le pickpocket *n'a que le ticket* — il n'a jamais connu votre mot de passe, qui n'a jamais quitté votre poche. Il ne peut pas le réciter : *le vol échoue*.

*Résumé des correspondances :*

|| Élément de l'analogie || Équivalent technique ||
|| Votre manteau | Les tokens (ID Token, Access Token) |
|| Le ticket numéroté | Le code d'autorisation |
|| Votre mot de passe secret | Le {{code_verifier}} |
|| L'empreinte du mot de passe | Le {{code_challenge}} (SHA-256 du code_verifier) |
|| Le préposé du vestiaire | Entra ID |

h3. 3.4 Schéma du flux Authorization Code avec PKCE (SPA)

!3.2-02-flux-spa-pkce.png|width=900!

{expand:Voir le code source PlantUML (pour modification future)}
{noformat}
@startuml
skinparam sequenceArrowThickness 2
skinparam roundcorner 10
skinparam noteBackgroundColor #E8F5E9

title Flux OIDC – SPA avec PKCE (sans client_secret)

actor "Utilisateur" as U #LightBlue
participant "Navigateur\n(SPA)" as B #LightGreen
participant "Entra ID\n(IdP)" as IdP #LightCoral

autonumber

U -> B : Accède à une route protégée\n(ex. /dashboard)

B -> B : 🎲 Génère un secret aléatoire :\n   **code_verifier** = "xK9mR2...aZ7" (43-128 chars)\n   **code_challenge** = BASE64URL(SHA256(code_verifier))
note right of B
  Ces deux valeurs sont générées
  **localement dans le navigateur**.
  Elles sont liées mathématiquement :
  on ne peut pas retrouver code_verifier
  à partir de code_challenge.
end note

B -> IdP : GET /authorize?\n  client_id=...\n  **code_challenge=aX7pQ2mZ...**\n  code_challenge_method=S256\n  redirect_uri=...\n  scope=openid profile\n  state=STATE\n  nonce=NONCE

IdP -> IdP : Mémorise le code_challenge\nassocié à cette tentative d'auth
IdP -> B : Affiche la page de connexion Microsoft
U -> IdP : Saisit email + mot de passe
IdP -> B : HTTP 302 → redirect_uri\n#code=CODE_UNIQUE&state=STATE

B -> B : ✅ Vérifie state (anti-CSRF)

B -> IdP : POST /token {\n  code=CODE_UNIQUE,\n  **code_verifier="xK9mR2...aZ7"**,\n  client_id=...,\n  redirect_uri=...\n  ⛔ PAS de client_secret\n}

IdP -> IdP : Calcule SHA256(code_verifier)\nVérifie == code_challenge reçu à l'étape 3
note right of IdP
  ✅ Si le hash correspond → OK
  ❌ Si quelqu'un a volé le code mais
     n'a pas le code_verifier → refusé
end note

IdP -> B : { id_token, access_token }
B -> B : Valide l'ID Token\n(signature JWKS, iss, aud, nonce, exp)
B -> U : Affiche /dashboard ✅

@enduml
{noformat}
{expand}

----

h2. 4. Pourquoi PKCE protège là où le code seul ne suffit pas

Ce schéma montre concrètement ce qui se passe si un attaquant intercepte le code d'autorisation :

!3.2-03-demo-attaque-pkce.png|width=800!

{expand:Voir le code source PlantUML (pour modification future)}
{noformat}
@startuml
skinparam sequenceArrowThickness 2
skinparam roundcorner 10
skinparam noteBackgroundColor #FFEBEE

title PKCE – Que se passe-t-il si quelqu'un vole le code ?

actor "SPA légitime" as SPA #LightGreen
actor "Attaquant" as Att #Salmon
participant "Entra ID" as IdP #LightCoral

autonumber

SPA -> IdP : GET /authorize?code_challenge=**"aX7pQ2mZ"**&...
IdP -> SPA : Code d'autorisation : **CODE123**

note over Att
  L'attaquant intercepte la redirection
  et récupère CODE123
  (ex. via une app malveillante installée
  sur le même appareil)
end note

Att -> IdP : POST /token { code=**CODE123**, ??? }
note right of IdP
  ❓ "Quel est le **code_verifier** ?"
  "SHA256(???) doit être égal à **aX7pQ2mZ**"
  L'attaquant n'a que le code volé, pas le code_verifier.
end note
IdP -> Att : ❌ **invalid_grant** — accès refusé

SPA -> IdP : POST /token { code=**CODE123**, code_verifier=**"xK9mR2...aZ7"** }
note right of IdP
  SHA256("xK9mR2...aZ7") == "aX7pQ2mZ" ✅
  La SPA prouve qu'elle est bien celle
  qui a initié la demande.
end note
IdP -> SPA : ✅ id_token + access_token

@enduml
{noformat}
{expand}

----

h2. 5. Cas particulier : PKCE sur une web app avec backend

Vous avez peut-être déjà vécu ça : *"j'ai une web app avec backend, mais on m'a quand même demandé d'utiliser PKCE"*. Maintenant que vous connaissez PKCE (section 3), voici pourquoi : PKCE et {{client_secret}} ne s'excluent pas — une web app peut utiliser les *deux en même temps*. Cela arrive dans 3 situations :

* *Entra ID l'impose* : l'App Registration a l'option *"Require PKCE"* activée par un administrateur IAM.
* *Votre bibliothèque OIDC l'ajoute automatiquement* : les librairies modernes (KnpU, league/oauth2, Spring Security...) génèrent PKCE par défaut depuis 2022-2023.
* *Bonne pratique de "sécurité en couches" (defense in depth)* : même avec un backend sécurisé, ajouter PKCE ne coûte rien et protège contre l'interception du code dans les logs ou l'historique navigateur.

La seule différence avec le flux SPA vu en section 3 : ici, le {{code_verifier}} est généré et stocké *côté serveur backend* — jamais dans le navigateur. La sécurité du {{client_secret}} n'est donc pas dégradée ; PKCE s'ajoute simplement en couche supplémentaire.

----

h2. 6. Web App vs SPA — Comparaison côte à côte

|| | *Web App (backend seul)* | *Web App + PKCE* | *SPA / App mobile* |
|| *Où s'exécute le code ?* | Serveur (privé) | Serveur (privé) | Navigateur (public) |
|| *Peut stocker un secret ?* | ✅ Oui | ✅ Oui | ❌ Non |
|| *{{client_secret}}* | ✅ Utilisé | ✅ Utilisé | ❌ Absent |
|| *PKCE* | ❌ Optionnel | ✅ Obligatoire (si Entra ID l'impose ou recommandé) | ✅ Obligatoire |
|| *Qui génère le code_verifier ?* | — | Le serveur backend | Le navigateur (SPA) |
|| *Le navigateur voit les tokens ?* | ❌ Non | ❌ Non | ✅ Oui (mémoire JS) |
|| *La requête /token est faite par...* | Le serveur backend | Le serveur backend | Le navigateur (SPA) |
|| *Niveau de sécurité* | Bon | ✅ Meilleur (double protection) | Bon (avec PKCE) |
|| *Protection CSRF* | {{state}} | {{state}} | {{state}} |
|| *Protection anti-rejeu* | {{nonce}} | {{nonce}} | {{nonce}} |

----

h2. 7. Le code_verifier en pratique — règles techniques

{noformat}
code_verifier :
  - Chaîne aléatoire de 43 à 128 caractères
  - Caractères autorisés : A-Z, a-z, 0-9, -, _, ., ~
  - Généré localement dans la SPA (ex. via crypto.getRandomValues)

code_challenge :
  - code_challenge = BASE64URL(SHA256(ASCII(code_verifier)))
  - Méthode : toujours "S256" (SHA-256) — ne pas utiliser "plain"
{noformat}

Exemple en JavaScript :

{code:javascript}
// Générer le code_verifier
function generateCodeVerifier() {
  const array = new Uint8Array(64);
  crypto.getRandomValues(array);
  return btoa(String.fromCharCode(...array))
    .replace(/\+/g, '-').replace(/\//g, '_').replace(/=/g, '');
}

// Calculer le code_challenge (SHA-256 + base64url)
async function generateCodeChallenge(verifier) {
  const data = new TextEncoder().encode(verifier);
  const digest = await crypto.subtle.digest('SHA-256', data);
  return btoa(String.fromCharCode(...new Uint8Array(digest)))
    .replace(/\+/g, '-').replace(/\//g, '_').replace(/=/g, '');
}

// Usage
const codeVerifier = generateCodeVerifier();
const codeChallenge = await generateCodeChallenge(codeVerifier);
// Stocker codeVerifier en sessionStorage (pas localStorage)
sessionStorage.setItem('pkce_verifier', codeVerifier);
{code}

----

h2. 8. Les 5 questions que tout le monde se pose

*"Pourquoi le navigateur fait-il une redirection vers Entra ID plutôt que de le contacter directement ?"*
Parce que l'utilisateur doit *saisir son mot de passe directement sur le site d'Entra ID* — pas sur votre app. Votre app ne voit jamais le mot de passe. C'est le principe fondamental du SSO : la délégation d'authentification.

*"Pourquoi y a-t-il un code intermédiaire et pas directement un token ?"*
Le code transite dans l'URL du navigateur (visible dans les logs, l'historique, les en-têtes HTTP Referer). Les tokens ne doivent pas y transiter. Le code est éphémère (~60s, usage unique) et ne vaut rien sans le secret pour l'échanger.

*"PKCE protège contre quoi exactement ?"*
Contre le vol du code d'autorisation par une application malveillante installée sur le même appareil (attaque "authorization code interception"). Sans PKCE sur une SPA, quelqu'un qui intercepte le code peut l'échanger contre des tokens — car il n'y a pas de {{client_secret}} à fournir.

*"Est-ce qu'une web app devrait aussi utiliser PKCE ?"*
Microsoft le recommande pour toutes les apps, mais ce n'est pas obligatoire si votre backend est sécurisé. Voir section 5 pour le détail des 3 cas où ça arrive en pratique.

*"Où stocker les tokens dans une SPA ?"*
En *mémoire JavaScript* (variable d'état React/Vue) de préférence. Pas en localStorage : une faille XSS (Cross-Site Scripting — injection de code malveillant dans la page) y donnerait accès à un attaquant. Pas en sessionStorage non plus si vous pouvez l'éviter, pour la même raison. L'Access Token doit avoir une durée de vie courte (5-60 min).

----

h2. 9. Récapitulatif visuel — Vue d'ensemble

!3.2-04-arbre-decision.png|width=800!

{expand:Voir le code source PlantUML (pour modification future)}
{noformat}
@startuml
skinparam roundcorner 15
skinparam defaultFontSize 13

title Vue d'ensemble – Choisir son flux OIDC

start

:L'application a-t-elle un backend\n(serveur privé, non exposé) ?;

if (OUI) then (Web App / API)
  :Flux Authorization Code\n**avec client_secret**;
  note right
    Le client_secret est stocké
    côté serveur uniquement.
    Le navigateur ne voit jamais les tokens.
  end note
  :PKCE optionnel\n(recommandé par Microsoft);
else (NON — SPA / App mobile)
  :Flux Authorization Code\n**avec PKCE obligatoire**;
  note right
    Pas de client_secret
    (le code source est public).
    PKCE prouve la continuité du flux.
  end note
  :Stocker les tokens en mémoire JS\n(pas en localStorage);
endif

:Valider l'ID Token :\n- Signature (JWKS)\n- iss, aud, nonce, exp;

:Utiliser l'Access Token\npour appeler les APIs protégées;

stop
@enduml
{noformat}
{expand}

----

h2. Références

* OAuth 2.0 authorization code flow (Microsoft) — https://learn.microsoft.com/en-us/entra/identity-platform/v2-oauth2-auth-code-flow
* SPA — authorization code flow with PKCE (Microsoft) — https://learn.microsoft.com/en-us/entra/identity-platform/v2-oauth2-auth-code-flow#request-an-authorization-code
* RFC 7636 — PKCE (IETF) — https://datatracker.ietf.org/doc/html/rfc7636
* *Dans ce projet* : *1.2 Guide OIDC – Gestion privée*, sections 3 (décision PKCE) et 4.1 à 4.5 (flux détaillé).
